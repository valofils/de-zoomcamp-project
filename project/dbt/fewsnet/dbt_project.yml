name: fewsnet
version: "1.0.0"
config-version: 2

profile: fewsnet

model-paths: ["models"]
test-paths:  ["tests"]
macro-paths: ["macros"]

target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  fewsnet:
    staging:
      +materialized: view
    marts:
      +materialized: table
